const NoResults = () => {
    return (
        <div
            className="No-results alert alert-warning text-center"
            role="alert"
        >
            No results match your search criteria.
        </div>
    );
};

export default NoResults;
